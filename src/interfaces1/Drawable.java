package interfaces1;

public interface Drawable {

    /** Draw this object on the canvas at the given (x, y) location. */
    public void drawOn(SimpleCanvas canvas, int locationX, int locationY);
}
